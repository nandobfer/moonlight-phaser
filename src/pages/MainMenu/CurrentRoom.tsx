import React from "react"
import { Box } from "@mui/material"

interface CurrentRoomProps {
    room: Room
}

export const CurrentRoom: React.FC<CurrentRoomProps> = ({ room }) => {
    return <Box sx={{}}></Box>
}
